export type PostType = {
  id: string;
  message: string;
  likesCount: number;
};

export type DialogItemType = {
  id: string;
  name: string;
};

export type MessageType = {
  id: string;
  message: string;
};
